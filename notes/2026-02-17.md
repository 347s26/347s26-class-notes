# 2026-02-17

## Notetakers

* section 1: Nate Baker
* section 2: 

## Agenda

1. Register a .tech domain name (this should be free with your github student developer pack promos)
    1. consider thinking about this for a while
        1. imagine i chose mcstewart.tech
    2. every student should do this
        1. we will leave the root of it alone for your own purposes (e.g. a personal professional profile)
        2. we will use like lib.mcstewart.tech to deploy your locallibrary
        3. game.mcstewart.tech will be your team's game (each teammate will deploy even though it's a shared codebase)
        4. project.mcstewart.tech witll be your final project (each team member will deploy even thought  it's a shared codebase)
2. Prep 5 Part 1 (DX: requirements, env, and data migration)
    1. Perhaps we should consider ignoring *.sqlite3...
        1. add `*.sqlite3` to your `.gitignore` file
        2. tell git to [delete it from the repo, but keep it in your local working copy](https://git-scm.com/docs/git-rm#Documentation/git-rm.txt---cached): with
            git rm --cached db.sqlite3
    3. in your localllibrary, add a README.md that lists the steps you must execute after cloning the repo to get it running locally
        1. should be able to clone
        2. delete sqlite database
        3. create new venv
        4. install from requirements
        5. migrate
        6. createsuperuser from env
        7. migrate
        8. tests:
            1. can run server
            2. can login to admin
            3. can expect certain books and other instance of the datamodel to exist
3. Games?

Domain Name: a human-language string that points to an IP Address.

a top level domain is the thing at the end (ex ".com", ".edu")

If I have a domain (ex: "hcientist.com"), then there are infinite subdomains addressable at "*.hcientist.com"

GitHub student dev pack will give a free domain.

Troubleshooting:
    pydantic.errors.PydanticSchemaGenerationError: Unable to generate pydantic-core schema for <class 'catalog.models.BookInstance'>. Set `arbitrary_types_allowed=True` in the model_config to ignore this error or implement `__get_pydantic_core_schema__` on your type to fully support it.

    If you got this error by calling handler(<some type>) within `__get_pydantic_core_schema__` then you likely need to call `handler.generate_schema(<some type>)` since we do not call `__get_pydantic_core_schema__` on `<some type>` otherwise to avoid infinite recursion.

    For further information visit https://errors.pydantic.dev/2.12/u/schema-for-unknown-type

    Check create_NAME/update_NAME method and ensure payload value is of type Schema, EX: create_language(request, payload: LanguageIn)

## Next time

1. 10 min: stand up a static frontend hosted by github pages, but with your custom domain name, e.g.
    1. static.testing.mcstewart.tech
2. have user interaction on template affect state in library so that you can apply this to your game
